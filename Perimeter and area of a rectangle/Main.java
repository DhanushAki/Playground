#include<stdio.h>
int main()
{
  int per,area;
  per = 2*(6+9);
  area = 6*9;
  printf("The perimeter of the rectangle is: %d cm\n",per);
  printf("The area of the rectangle is: %d sq cm\n",area);
  return 0;
}